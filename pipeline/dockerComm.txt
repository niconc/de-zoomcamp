# Keep-alive script:
 while true; do sleep 300; echo "Keep-alive ping: $(date)"; done

# docker pipeline:
docker run -it --rm \
    -e POSTGRES_USER="root" \
    -e POSTGRES_PASSWORD="root" \
    -e POSTGRES_DB="ny_taxi" \
    -v ny_taxi_postgres_data:/var/lib/postgresql \
    -p 5432:5432 \
    postgres:18


#uv pipeline (Postgress):
uv run pgcli -h localhost -u root -p 5432 -d ny_taxi

# uv pipeline (Jupyter):
uv run jupyter notebook // (Classic Notebook v6/v7 style)
uv run jupyter lab // (LSP - Language Server Protocol to use intellisense)
